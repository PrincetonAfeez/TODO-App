name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -r requirements.txt
      - run: python -m ruff check .
      - run: python -m black --check config tasks
      - run: python -m mypy tasks/services.py tasks/models.py
      - run: node --test static/tasks/toggle-helpers.test.js static/tasks/theme-helpers.test.js
      - run: python -m pytest -m "not e2e" --cov=tasks --cov-report=term-missing --cov-fail-under=90

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -r requirements.txt
      - run: python -m playwright install chromium
      - run: python -m pytest -m e2e
